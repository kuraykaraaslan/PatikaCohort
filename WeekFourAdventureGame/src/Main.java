import Game.Game;

public class Main {
    public static void main(String[] args) {
        Game game = new Game(); // Creating an instance of the Game class and assigning it to the "game" variable
        game.start(); // Calling the start method on the "game" object
    }
}
